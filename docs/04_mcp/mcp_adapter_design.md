# MCP Adapter 设计

## 设计目标

- 定义适配层职责与边界。
- 确保外部工具具备一致的内部表现。

## 核心概念

- **Adapter**：将 MCP 工具元数据与调用协议映射为 Skill Manifest 与 Runtime 调用。
- **Normalization**：统一输入输出与错误模型。

## 适配职责

- 映射 MCP 工具描述为 Skill Manifest。
- 强制执行权限与预算策略。
- 统一错误模型与日志格式。

## 非目标

- 不定义具体缓存或重试实现。

## 未来演进方向

- 支持多 MCP 版本的适配器自动协商。
