# 预算与限制

## 设计目标

- 通过统一预算模型限制资源消耗。
- 保障跨工具与多端调用的一致治理。

## 核心概念

- **预算单位**：抽象成本单位，可映射为时间、token 或费用。
- **预算策略**：按用户、会话、项目或任务划分的预算规则。
- **限制类型**：次数限制、并发限制、成本上限。

## 设计约束

- 每次调用必须声明预算级别。
- 预算不足必须显式反馈给用户或管理员。
- 预算策略需可审计、可回放。

## 非目标

- 不定义具体成本核算算法。

## 未来演进方向

- 支持动态预算调整与策略推荐。
