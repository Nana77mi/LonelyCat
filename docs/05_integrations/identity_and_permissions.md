# 身份与权限

## 设计目标

- 统一多端身份的权限控制。
- 保障工具调用与事实访问的安全性。

## 核心概念

- **User Identity**：平台内部统一身份。
- **Role/Policy**：权限策略与角色定义。
- **Permission Check**：在工具调用、事实读写与消息发送时执行。

## 设计约束

- 权限检查必须在 Core API 层完成。
- 任何权限拒绝必须显式告知调用方并记录审计。

## 非目标

- 不定义具体权限模型实现（RBAC/ABAC 等细节）。

## 未来演进方向

- 支持跨组织权限隔离与细粒度策略。
