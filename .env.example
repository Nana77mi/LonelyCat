# Web Console API base URL
# - Leave empty/unset for dev: uses /api proxy to http://127.0.0.1:8000 (via Vite)
# - Set full URL for production: e.g., http://localhost:8000 or https://api.example.com
VITE_CORE_API_URL=http://localhost:8000

# Alternative: VITE_API_BASE_URL (lower priority than VITE_CORE_API_URL)
# VITE_API_BASE_URL=/api
