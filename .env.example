# Core API Port (for Vite proxy configuration)
# - Used by vite.config.ts to configure /api proxy target
# - Default: 5173 (matches Makefile API_PORT)
# - If core-api runs on a different port (e.g., 8001 when 8000 is occupied), set this accordingly
CORE_API_PORT=5173

# Web Console API base URL (optional, overrides CORE_API_PORT)
# - Leave empty/unset for dev: uses /api proxy to http://127.0.0.1:<CORE_API_PORT> (via Vite)
# - Set full URL for production: e.g., http://localhost:5173 or https://api.example.com
# VITE_CORE_API_URL=http://localhost:5173

# Alternative: VITE_API_BASE_URL (lower priority than VITE_CORE_API_URL)
# VITE_API_BASE_URL=/api

# Sandbox workspace for run_code_snippet (python.run / shell.run). Required for "会话驱动沙箱跑代码".
# - Windows: set to a local path, e.g. D:\Project\lonelycat\LonelyCat\workspace (create the folder first)
# - Or configure in Web Console: Settings -> sandbox.workspace_root_win / workspace_root_wsl
# SANDBOX_WORKSPACE_ROOT_WIN=D:\Project\lonelycat\workspace
