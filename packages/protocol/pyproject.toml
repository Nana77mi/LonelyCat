[project]
name = "lonelycat-protocol"
version = "0.1.0"
description = "Shared Pydantic schemas for LonelyCat"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.6",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
addopts = "-q"
