[project]
name = "lonelycat-agent-worker"
version = "0.1.0"
description = "Background worker for LonelyCat"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.6",
]

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
addopts = "-q"
